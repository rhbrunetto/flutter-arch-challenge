name: crypto_challenge
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.13.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_modular: ^4.0.0+11
  bloc: ^7.0.0
  flutter_bloc: ^7.0.0
  dio: ^4.0.0
  intl: ^0.17.0
  dartz: ^0.10.0
  currency_text_input_formatter: ^2.1.3

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
  mocktail: ^0.1.4

flutter:
  uses-material-design: true
  assets:
    - assets/json/
